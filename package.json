{
  "name": "mindhub-backend-root",
  "version": "1.0.0",
  "description": "MindHub Backend - Root configuration for Vercel",
  "scripts": {
    "install": "echo 'Installing backend dependencies...' && cd mindhub/backend && npm install",
    "build": "echo 'Building backend...' && cd mindhub/backend && npm run generate:clean",
    "start": "cd mindhub/backend && npm start",
    "dev": "cd mindhub/backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {}
}