{
  "name": "mindhub-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start": "cd mindhub/backend && npm start",
    "build": "cd mindhub/backend && npm install",
    "install": "cd mindhub/backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "task-master-ai": "^0.19.0"
  },
  "devDependencies": {
    "prisma": "^6.13.0"
  }
}
