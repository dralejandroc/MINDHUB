{
  "name": "mindhub-backend-root",
  "version": "1.0.0",
  "description": "MindHub Backend - Root configuration for Vercel",
  "scripts": {
    "build": "echo 'Debugging directory structure...' && ls -la && echo 'Checking mindhub directory...' && ls -la mindhub && echo 'Installing backend...' && cd mindhub/backend && npm install && npm run generate:clean",
    "start": "cd mindhub/backend && npm start",
    "dev": "cd mindhub/backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {}
}